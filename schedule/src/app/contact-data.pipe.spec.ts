import { ContactDataPipe } from './contact-data.pipe';

describe('ContactDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactDataPipe();
    expect(pipe).toBeTruthy();
  });
});
