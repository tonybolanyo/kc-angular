import { ContactsSortPipe } from './contacts-sort.pipe';

describe('ContactsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
